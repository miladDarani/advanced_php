# A Sample Node and Composer Project

Before you begin, type the following commands in Bash, inside the project folder:

```bash
npm install
composer install
```

This will install all the projects Node and Composer dependencies.


